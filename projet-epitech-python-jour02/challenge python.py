import math
diviseurs = range (1, 21)
resultat = 1
for d in diviseurs:
    resultat = math.lcm(resultat, d)
print(resultat)

import math
diviseurs = range (1, 201)
resultat = 1
for d in diviseurs:
    resultat = math.lcm(resultat, d)
print(resultat)

import math
diviseurs = range (1, 2001)
resultat = 1
for d in diviseurs:
    resultat = math.lcm(resultat, d)
print(resultat)